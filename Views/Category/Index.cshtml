@model IEnumerable<Rocky_Models.Category>


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
<h2 class="text-primary">Category list</h2>
        </div>
        <div class="col-6  text-end">
            <a class="btn btn-primary" asp-controller="Category" asp-action="Create" >
                <i class="fa-solid fa-plus"></i>&nbsp;
                Create new Category
            </a>
        </div>
    </div>
<br/><br/>
@if(Model.Count()>0){
    <table class = table table-bordered table-striped style="width: 100%;">
        <thead>
            <tr>
                <th>Category Name</th>
                <th>Display Order</th>
                <th>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach(var obj in Model){
                <tr>
                    <td width:"50%">@obj.Name</td>
                    <td width:"30%">@obj.DisplayOrder</td>
                    <td class="text-center">
                        <div class="w-75 btn-group" role="group">
                            <a class="btn btn-primary mx-2" asp-controller="Category" asp-route-ID="@obj.Id" asp-action="Edit" >
                               <i class="fa-regular fa-pen-to-square"></i> 
                            </a>
                            <a class="btn btn-danger mx-2" asp-controller="Category" asp-route-ID="@obj.Id" asp-action="Delete" >
                               <i class="fa-solid fa-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}else{
    <p>No category exists.</p>
}
</div>